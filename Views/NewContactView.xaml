<Window x:Class="WpfMessenger.Views.NewContactView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMessenger.Views"
        mc:Ignorable="d"
        Title="NewContactView" Height="190" Width="235" ResizeMode="NoResize" 
        Background="#FF333131" FontFamily="Century Gothic" WindowStartupLocation="CenterScreen">
    <Grid>
        <Label Margin="10,0,10.333,125" Content="Add Contact" Style="{StaticResource Title}"/>
        <Label Margin="10,40,0,0" Content="Name" Style="{StaticResource Field}"/>
        <TextBox x:Name="tbName" Margin="85,42,10,0"/>
        <Label Margin="10,75,0,0" Content="Number" Style="{StaticResource Field}"/>
        <TextBox x:Name="tbNumber" Margin="85,75,10,0"/>
        <Button x:Name="btnAccept" Margin="171,110,19.333,14" Click="BtnAccept_Click" 
                Template="{StaticResource OkButton}"/>
        <Button x:Name="btnBack" Background="Transparent" BorderBrush="Transparent"
                Click="BtnBack_Click" Margin="10,110,180,10">
            <Image Source="/Style/back.png"/>
        </Button>
    </Grid>
</Window>
